# OpenCode Tailnet Launcher v0.0.12

- Synced the public repo to the stable modular router baseline with disk cache recovery, watcher-driven refresh, SSE updates, and offline-ready launch behavior.
- Updated the public launcher/docs so `router_url` examples keep `autogo=1` and the launcher injects the current Tailscale host and port automatically.
